//it gives the first index occurence of a string or a single alphabet of string
// and returns -1 if the word or alphabet passed through it is not found 
//syntax ->   stringName.indexOf(arg);
let str="ILoveCoding";
str.indexOf("o");//2
str.indexOf("Love");//1
str.indexOf("a");//-1